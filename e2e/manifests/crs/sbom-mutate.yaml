apiVersion: policy.uds.dev/v1alpha1
kind: SBOMEnforcement
metadata:
  name: sbom-mutate-test
spec:
  namespaces:
    - e2e-sbom-mutate
  enforcementPolicy:
    mode: enforce
  deniedComponents:
    - name: nonexistent-package-that-wont-match
      versionRange: "*"
