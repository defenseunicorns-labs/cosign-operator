apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sbomenforcements.policy.uds.dev
spec:
  group: policy.uds.dev
  scope: Cluster
  names:
    plural: sbomenforcements
    singular: sbomenforcement
    kind: SbomEnforcement
    shortNames:
      - sbom
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - namespaces
                - deniedComponents
                - enforcementPolicy
              properties:
                namespaces:
                  type: array
                  minItems: 1
                  items:
                    type: string
                    minLength: 1
                  x-kubernetes-validations:
                    - rule: "self.all(ns, ns != 'pepr-system' && ns != 'istio-system' && ns != 'zarf' && ns != 'kube-system')"
                      message: "namespaces cannot include pepr-system, istio-system, zarf, or kube-system"
                deniedComponents:
                  type: array
                  items:
                    type: object
                    required:
                      - name
                      - versionRange
                    properties:
                      name:
                        type: string
                      versionRange:
                        type: string
                enforcementPolicy:
                  type: object
                  required:
                    - mode
                  properties:
                    mode:
                      type: string
                      enum:
                        - enforce
                        - warn
