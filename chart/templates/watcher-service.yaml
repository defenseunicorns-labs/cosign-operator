{{- if .Values.watcher.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: pepr-image-signature-policy-watcher
  namespace: pepr-system
  labels:
    pepr.dev/controller: watcher
spec:
  selector:
    app: pepr-image-signature-policy-watcher
    pepr.dev/controller: watcher
  ports:
  - port: 443
    targetPort: 3000
{{- end }}
    